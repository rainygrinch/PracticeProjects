# right a function that takes two arguments (weekday and vacation)
# and returns true (so we sleep in) if weekday is false or vacation is true

def sleep_in(weekday, vacation):
    if weekday == False or vacation == True:
        return True
    else
        return False
